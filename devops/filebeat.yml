filebeat.prospectors:
- input_type: log
  paths:
    - /root/apps/nodebb/logs/output.log
  fields:
    project: nodebb
    env: ${PROJECT_ENV}
output.logstash:
  hosts: ["${LOGSTASH_HOST}"]
logging.level: debug
