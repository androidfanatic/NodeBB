metricbeat.modules:
  - module: system
    metricsets:
      - cpu
      - filesystem
      - memory
    enabled: true
    period: 10s
    cpu_ticks: false
fields:
  project: nodebb
  env: ${CHATOPS_PROJECT_ENV}
output.logstash:
  hosts: ["${CHATOPS_LOGSTASH_HOST}"]
